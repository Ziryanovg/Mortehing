import QtQuick 2.0

ListModel
{
    ListElement{text:"f(x) = A*(x*x) + B*x + C"}

    ListElement{text:"f(x) = A * sin(x) + B * cos( C*x )"}

    ListElement{text:"f(x) = A*log( B*x )"}

    ListElement{text:"f(x) = A / ( sin(x*x) * B )"}
}
